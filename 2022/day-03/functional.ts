import {
  option as O,
  either as E,
  string as S,
  number as N,
  readonlyArray as A,
  readonlyNonEmptyArray as NEA,
  boolean as B,
  function as F,
} from "fp-ts";

export const functionalProgramA = (input: string) => null;

export const functionalProgramB = (input: string) => null;
